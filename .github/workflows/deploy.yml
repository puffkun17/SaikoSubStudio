name: Deploy SaikoSubStudio to Cloudflare Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:   # Allow manual trigger from GitHub UI

# Node.js 20 deprecation handling (GitHub Actions runner).
# GitHub is deprecating Node 20 for action execution (forced Node 24 from mid-2026).
# We use the unsecure opt-out temporarily because the project is pinned to
# Next.js 15.5.2 + @cloudflare/next-on-pages ^1.13 for Cloudflare compatibility.
# See: https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/
#
# When the CF adapter supports newer Next.js + Node 24, remove the env below
# and consider upgrading node-version here + removing --legacy-peer-deps.
env:
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        # --legacy-peer-deps is required for compatibility with current
        # @cloudflare/next-on-pages + Next 15.5.2 pin.
        run: npm ci --legacy-peer-deps

      - name: Build for Cloudflare Pages
        run: npm run pages:build

      # Use direct wrangler (from devDependencies) instead of wrangler-action
      # for better error logs and to avoid action-internal npx issues.
      # This also makes the command use the Node version we just set up.
      - name: Deploy to Cloudflare Pages
        run: npx wrangler pages deploy .vercel/output/static --project-name=saikosubstudio
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        # Required secret: CLOUDFLARE_API_TOKEN with "Pages:Edit" scope.
        # If you see auth errors, create the secret in repo Settings → Secrets and variables → Actions.

      - name: Deployment info
        run: |
          echo "✅ Deployment step completed."
          echo "Check the Cloudflare Pages dashboard for the live URL."
          echo "Project: saikosubstudio"
